<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="id.ac.polinema.absenguruprivate">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SiswaActivity"></activity>

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyDGyWPwfZXoaz2AsceT_1j95SlUMksztgc"/>

        <activity android:name=".TambahGuruActivity" />
        <activity android:name=".Guru1Activity"/>
        <activity android:name=".AdminActivity" />
        <activity android:name=".GuruActivity" />
        <activity android:name=".TambahSiswaActivity"/>
        <activity android:name=".MapsActivity"/>
<!--        <activity android:name=".MainActivity" />-->
        <activity android:name=".LoginGuruActivity" />
        <activity android:name=".LoginAdminActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>